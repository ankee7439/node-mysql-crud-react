# node-mysql-crud-react
## All are running on the same port and with the prefix 'books'
### Clone thing and make '.env' file with some port number
## Make dataBase on your mySql and import the 'books.sql' file
